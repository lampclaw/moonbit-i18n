import {
  "lampclaw/i18n/runtime",
}

import {
  "moonbitlang/core/bench",
} for "test"

options(
  targets: {
    "formatter.mbt": [ "js" ],
    "formatter_portable.mbt": [ "native", "wasm", "wasm-gc", "llvm" ],
  },
)
