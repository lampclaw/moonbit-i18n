// Code generated by scripts/generate-mf2-syntax-tests.mjs; DO NOT EDIT.
// Unicode MessageFormat WG fixtures: 7f142fb4f1f5ea6ab1eb34ce2b87e918ca9fd331.

///|
test "Unicode MF2 pinned well-formed syntax fixtures" {
  let sources = [
    "", "a", "hello", "\\\\", "\\\\\\{\\|\\}", "hello {world}", "hello {|world|}",
    "\n hello\t", "hello {$place}", "hello {$place-.}", "hello {$place}", "{a}",
    "{a :f}", "{a :u:f}", "{$x}", "{$x :f}", "{$x :u:f}", "{:u:f}", "{:f}", "{{}}",
    "{#tag}", ".input{$x}{{}}", "\t.input{$x}{{}}\n", ".input{$x}.input{$y}{{}}",
    ".input{$x} .input{$y}{{}}", "  {{}}  ", ".local $x ={a}.input{$y}{{}}", ".local $a={a :f}.match $a a{{}}*{{}}",
    ".input {$x}{{}}", ".local $x ={a}{{}}", ".local $x= {a}{{}}", ".local $x = {a}{{}}",
    "{{.input {$x}}}", "{{ .local $x = {$y}}}", ".local $a={a :f}.local $b={b :f}.match $a $b a b{{}}* *{{}}",
    ".local $a={a :f}.match $a a{{}}b{{}}*{{}}", ".local $a={a :f}.match $a a {{}}*{{}}",
    ".local $a={a :f}.local $b={b :f}.match $a $b a b {{}}* *{{}}", ".local $a={a :f}.match $a *{{}}",
    "{ a}", "{a @c}", "{a }", "{ $x}", "{$x @c}", "{$x }", "{ :f}", "{:f @c}", "{:f }",
    "{ #a}", "{#tag foo=bar}", "{#a @c}", "{#a }", "{#a/}", "{/a}", "{ /a}", "{/tag foo=bar}",
    "{/a }", "{:f k=v}", "{:f k =v}", "{:f k= v}", "{:f k = v}", "{a @c=d}", "{a @c =d}",
    "{a @c= d}", "{a @c = d}", "{42 @foo=|bar|}", "{||}", "{|a|}", "{|\\\\|}", "{|a\\\\\\{\\|\\}|}",
    "{0}", "{-0}", "{1}", "{11}", "{0.1}", "{0.12}", "{0e1}", "{0E1}", "{0E-1}",
    "{0E-1}", "{+}", "{-}", "{·}", "{🥔}", "{🀄️}", "{🏳️‍🌈}", "{¡؝ᚁ​‐‰⁠⁪、ﷰ}",
    "hello { world\t\n}", "hello {　world\r}", "{$one} and {$two}", "{$one} et {$two}",
    ".local $foo = {bar} {{bar {$foo}}}", ".local $foo = {|bar|} {{bar {$foo}}}",
    ".local $foo = {|bar|} {{bar {$foo}}}", ".local $foo = {$bar} {{bar {$foo}}}",
    ".local $foo = {$baz} .local $bar = {$foo} {{bar {$bar}}}", ".input {$foo} {{bar {$foo}}}",
    ".input {$foo} .local $bar = {$foo} {{bar {$bar}}}", ".local $foo = {$baz} .local $bar = {$foo} {{bar {$bar}}}",
    ".local $x = {42} .local $y = {$x} {{{$x} {$y}}}", "{#tag}content", "{#ns:tag}content{/ns:tag}",
    "{/tag}content", "{#tag foo=bar/}", "{#tag a:foo=|foo| b:bar=$bar}", "{42 @foo @bar=13}",
    "{{trailing whitespace}} \n", "Ḍ̇", ".local $Ḍ̇ = {foo} {{{$Ḍ̇}}}",
    ".local $Ḍ̇ = {foo} {{{$Ḍ̇}}}", ".input {$Ḍ̇} {{{$Ḍ̇}}}", ".input {$Ḍ̇} {{{$Ḍ̇}}}",
    ".local $Ḍ̇ = {foo} {{{$Ḍ̇}}}", ".local $Ǻ = {foo} {{{$Ǻ}}}",
  ]
  let failures : Array[String] = []
  for source in sources {
    match parse_mf2_syntax(source) {
      Ok(_) => ()
      Err(error) => failures.push(source + " => " + error.message)
    }
  }
  if !failures.is_empty() {
    fail("expected pinned well-formed syntax:\n" + failures.join("\n"))
  }
}

///|
test "Unicode MF2 pinned syntax-error fixtures" {
  let sources = [
    ".", "{", "}", "{}", "{{", "{{}", "{{}}}", "{|foo| #markup}", "{{missing end brace}",
    "{{missing end braces", "{{missing end {$braces", "{{extra}} content", "empty { } placeholder",
    "missing space {42:func}", "missing space {|foo|:func}", "missing space {|foo|@bar}",
    "missing space {:func@bar}", "missing space {:func @bar@baz}", "missing space {:func @bar=42@baz}",
    "missing space {+reserved@bar}", "missing space {&private@bar}", "bad {:} placeholder",
    "bad {\\u0000placeholder}", "no-equal {|42| :number minimumFractionDigits 2}",
    "bad {:placeholder option=}", "bad {:placeholder option value}", "bad {:placeholder option:value}",
    "bad {:placeholder option}", "bad {:placeholder:}", "bad {::placeholder}", "bad {:placeholder::foo}",
    "bad {:placeholder option:=x}", "bad {:placeholder :option=x}", "bad {:placeholder option::x=y}",
    "bad {$placeholder option}", "bad {:placeholder @attribute=}", "bad {:placeholder @attribute=@foo}",
    "bad {:placeholder @attribute=$foo}", "{ @misplaced = attribute }", "no {placeholder end",
    "no {$placeholder end", "no {:placeholder end", "no {|placeholder| end", "no {|literal} end",
    "no {|literal or placeholder end", ".local bar = {|foo|} {{_}}", ".local #bar = {|foo|} {{_}}",
    ".local $bar {|foo|} {{_}}", ".local $bar = |foo| {{_}}", ".match {{foo}}", ".match * {{foo}}",
    ".match x * {{foo}}", ".match |x| * {{foo}}", ".match :x * {{foo}}", ".match {$foo} * {{foo}}",
    ".match {#foo} * {{foo}}", ".input {$x :x} .match {$x} * {{foo}}", ".input {$x :x} .match$x * {{foo}}",
    ".input {$x :x} .match $x* {{foo}}", ".input {$x :x} .match $x|x| {{foo}} * {{foo}}",
    ".input {$x :x} .local $y = {y :y} .match $x$y * * {{foo}}", ".input {$x :x} .local $y = {y :y} .match $x $y ** {{foo}}",
    ".input {$x :x} .match $x", ".input {$x :x} .match $x *", ".input {$x :x} .match $x * foo",
    ".input {$x :x} .match $x * {{foo}} extra", ".n{a}{{}}", "{^}", "{!}", ".n .{a}{{}}",
    ".n. {a}{{}}", ".n.{a}{b}{{}}", "{!.}", "{! .}", "{%}", "{*}", "{<}", "{>}",
    "{?}", "{~}", "{^.}", "{^ .}", "{&}", "{﷐}", "{￾}", "{!.\\{}", "{!. \\{}",
    "{!|a|}", "foo {&private}", "foo {?reserved @a @b=c}", ".foo {42} {{bar}}", ".foo{42}{{bar}}",
    ".foo |}lit{| {42}{{bar}}", ".i {1} {{}}", ".l $y = {|bar|} {{}}", ".l $x.y = {|bar|} {{}}",
    "hello {|4.2| %number}", "hello {|4.2| %n|um|ber}", "hello {|4.2| &num|be|r}",
    "hello {|4.2| ^num|be|r}", "hello {|4.2| +num|be|r}", "hello {|4.2| ?num|be||r|s}",
    "hello {|foo| !number}", "hello {|foo| *number}", "hello {?number}", "{<tag}",
    ".local $bar = {$none ~plural} .match {foo :string}  * {{{$bar}}}", "hello {|4.2| %num\\\\ber}",
    "hello {|4.2| %num\\{be\\|r}", "hello {|4.2| %num\\\\\\}ber}", "hello {|4.2| !}",
    "hello {|4.2| %}", "hello {|4.2| *}", "hello {|4.2| ^abc|123||5|\\\\}", "hello {|4.2| ^ abc|123||5|\\\\}",
    "hello {|4.2| ^ abc|123||5|\\\\ \\|def |3.14||2|}", "hello {|4.2| ? }", "hello {|4.2| %xyzz }",
    "hello {|4.2| >xyzz   }", "hello {$foo ~xyzz }", "hello {$x   <xyzz   }", "{>xyzz }",
    "{  !xyzz   }", "{~xyzz }", "{ <xyzz   }", "hello {|4.2| !xy z z }", "hello {|4.2| *num \\\\ b er}",
    "hello {|4.2| %num \\\\ b |3.14| r    }", "hello {|4.2|    +num xx \\\\ b |3.14| r  }",
    "hello {$foo    +num x \\\\ abcde |3.14| r  }", "hello {$foo    >num x \\\\ abcde |aaa||3.14||42| r  }",
    "hello {$foo    >num x \\\\ abcde |aaa||3.14| |42| r  }", ".input{ $n ~ }{{{$n}}}",
  ]
  let failures : Array[String] = []
  for source in sources {
    match parse_mf2_syntax(source) {
      Err(error) if error.code == Mf2SyntaxCode => ()
      Err(error) => failures.push(source + " => wrong code " + error.message)
      Ok(_) => failures.push(source)
    }
  }
  if !failures.is_empty() {
    fail("expected pinned syntax errors:\n" + failures.join("\n"))
  }
}

///|
test "Unicode MF2 pinned data-model-error fixtures" {
  let model_0 = match
    parse_mf2_syntax(".input {$foo :x} .match $foo * * {{foo}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 0: " + error.message)
  }
  let errors_0 = validate_mf2_model(model_0)
  assert_true(errors_0.any(error => error.code == Mf2VariantKeyMismatchCode))
  let model_1 = match
    parse_mf2_syntax(
      ".input {$foo :x} .input {$bar :x} .match $foo $bar * {{foo}}",
    ) {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 1: " + error.message)
  }
  let errors_1 = validate_mf2_model(model_1)
  assert_true(errors_1.any(error => error.code == Mf2VariantKeyMismatchCode))
  let model_2 = match parse_mf2_syntax(".input {$foo :x} .match $foo 1 {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 2: " + error.message)
  }
  let errors_2 = validate_mf2_model(model_2)
  assert_true(
    errors_2.any(error => error.code == Mf2MissingFallbackVariantCode),
  )
  let model_3 = match
    parse_mf2_syntax(".input {$foo :x} .match $foo other {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 3: " + error.message)
  }
  let errors_3 = validate_mf2_model(model_3)
  assert_true(
    errors_3.any(error => error.code == Mf2MissingFallbackVariantCode),
  )
  let model_4 = match
    parse_mf2_syntax(
      ".input {$foo :x} .input {$bar :x} .match $foo $bar * 1 {{_}} 1 * {{_}}",
    ) {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 4: " + error.message)
  }
  let errors_4 = validate_mf2_model(model_4)
  assert_true(
    errors_4.any(error => error.code == Mf2MissingFallbackVariantCode),
  )
  let model_5 = match
    parse_mf2_syntax(".input {$foo} .match $foo one {{one}} * {{other}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 5: " + error.message)
  }
  let errors_5 = validate_mf2_model(model_5)
  assert_true(
    errors_5.any(error => error.code == Mf2MissingSelectorAnnotationCode),
  )
  let model_6 = match
    parse_mf2_syntax(".local $foo = {$bar} .match $foo one {{one}} * {{other}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 6: " + error.message)
  }
  let errors_6 = validate_mf2_model(model_6)
  assert_true(
    errors_6.any(error => error.code == Mf2MissingSelectorAnnotationCode),
  )
  let model_7 = match
    parse_mf2_syntax(
      ".input {$bar} .local $foo = {$bar} .match $foo one {{one}} * {{other}}",
    ) {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 7: " + error.message)
  }
  let errors_7 = validate_mf2_model(model_7)
  assert_true(
    errors_7.any(error => error.code == Mf2MissingSelectorAnnotationCode),
  )
  let model_8 = match parse_mf2_syntax(".input {$foo} .input {$foo} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 8: " + error.message)
  }
  let errors_8 = validate_mf2_model(model_8)
  assert_true(errors_8.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_9 = match
    parse_mf2_syntax(".input {$foo} .local $foo = {42} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 9: " + error.message)
  }
  let errors_9 = validate_mf2_model(model_9)
  assert_true(errors_9.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_10 = match
    parse_mf2_syntax(".local $foo = {42} .input {$foo} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 10: " + error.message)
  }
  let errors_10 = validate_mf2_model(model_10)
  assert_true(errors_10.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_11 = match
    parse_mf2_syntax(".local $foo = {:unknown} .local $foo = {42} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 11: " + error.message)
  }
  let errors_11 = validate_mf2_model(model_11)
  assert_true(errors_11.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_12 = match
    parse_mf2_syntax(".local $foo = {$bar} .local $bar = {42} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 12: " + error.message)
  }
  let errors_12 = validate_mf2_model(model_12)
  assert_true(errors_12.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_13 = match parse_mf2_syntax(".local $foo = {$foo} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 13: " + error.message)
  }
  let errors_13 = validate_mf2_model(model_13)
  assert_true(errors_13.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_14 = match
    parse_mf2_syntax(".local $foo = {$bar} .local $bar = {$baz} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 14: " + error.message)
  }
  let errors_14 = validate_mf2_model(model_14)
  assert_true(errors_14.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_15 = match
    parse_mf2_syntax(".local $foo = {$bar :func} .local $bar = {$baz} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 15: " + error.message)
  }
  let errors_15 = validate_mf2_model(model_15)
  assert_true(errors_15.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_16 = match
    parse_mf2_syntax(".local $foo = {42 :func opt=$foo} {{_}}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 16: " + error.message)
  }
  let errors_16 = validate_mf2_model(model_16)
  assert_true(errors_16.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_17 = match
    parse_mf2_syntax(
      ".local $foo = {42 :func opt=$bar} .local $bar = {42} {{_}}",
    ) {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 17: " + error.message)
  }
  let errors_17 = validate_mf2_model(model_17)
  assert_true(errors_17.any(error => error.code == Mf2DuplicateDeclarationCode))
  let model_18 = match
    parse_mf2_syntax("bad {:placeholder option=x option=x}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 18: " + error.message)
  }
  let errors_18 = validate_mf2_model(model_18)
  assert_true(errors_18.any(error => error.code == Mf2DuplicateOptionNameCode))
  let model_19 = match
    parse_mf2_syntax("bad {:placeholder ns:option=x ns:option=y}") {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 19: " + error.message)
  }
  let errors_19 = validate_mf2_model(model_19)
  assert_true(errors_19.any(error => error.code == Mf2DuplicateOptionNameCode))
  let model_20 = match
    parse_mf2_syntax(
      ".input {$var :string} .match $var * {{The first default}} * {{The second default}}",
    ) {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 20: " + error.message)
  }
  let errors_20 = validate_mf2_model(model_20)
  assert_true(errors_20.any(error => error.code == Mf2DuplicateVariantCode))
  let model_21 = match
    parse_mf2_syntax(
      ".input {$x :string} .input {$y :string} .match $x $y * foo {{The first foo variant}} bar * {{The bar variant}} * |foo| {{The second foo variant}} * * {{The default variant}}",
    ) {
    Ok(value) => value
    Err(error) =>
      fail("expected well-formed pinned data model 21: " + error.message)
  }
  let errors_21 = validate_mf2_model(model_21)
  assert_true(errors_21.any(error => error.code == Mf2DuplicateVariantCode))
  guard parse_valid_mf2_model(
      ".local $star = {star :string} .match $star |*| {{Literal star}} * {{The default}}",
    )
    is Ok(_) else {
    fail("expected valid pinned data model 22")
  }
}
