# `lampclaw/i18n` 0.2.x acceptance record

Date: 2026-08-14

## Accepted release: `0.2.1`

Immutable identity:

- release commit: `3cf795119c56da758e3fc49def9c9071c62f26ae`;
- signed tag: `v0.2.1`;
- tag signature: Lampclaw ED25519 key
  `SHA256:YrC/9aayrFCWl773eSEhE6G3FgROFEc/VQmxTawowDA`;
- archive SHA-256:
  `1b146cbeb2cf29e73f52f49ea42b200446d4d63ff568b776bc01a3562a5277dc`;
- release-commit Test workflow:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31766943287>;
- signed-tag Test workflow:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31767995394>;
- signed-tag Linux/macOS/Windows Registry smoke:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31767995400>; and
- registry page:
  <https://mooncakes.io/docs/#/lampclaw/i18n/0.2.1>.

The real `moon publish` command was invoked exactly once for `0.2.1`. It ran
from `2026-08-14T03:32:25Z` through `2026-08-14T03:32:27Z`, exited zero, and
reported `Server status: 200 OK`. No retry was attempted. The preceding dry
run passed both archive checks and received the exact version-specific `202
Accepted` success detail. Moon `0.1.20260807` with mooncake-bin
`0.1.20260805` still mapped that successful dry run to exit 255; the release
helper accepted it only after all documented evidence matched.

Mooncakes later reported build status `success`, version `0.2.1`, the expected
repository and Apache-2.0 license, and the exact archive checksum above. Its
rendered README, metadata, source archive, generator API, runtime API,
JavaScript formatter API, and prebuilt CLI asset passed verification. The
registry build remained queued for approximately seventeen minutes; no
publish retry was attempted while waiting.

The exact-version clean-module smoke passed:

- `moon add lampclaw/i18n@0.2.1`;
- `moonx lampclaw/i18n/cmd/i18n@0.2.1` and optional `moon add --bin`;
- atomic bilingual scaffold with English embedded and Chinese dynamic;
- invalid MF2 repair cycle with JSON diagnostic `I18N3001`, source path, and
  span;
- deterministic generation manifest and repeated true no-op;
- exactly two final destination locks and no staging locks;
- JavaScript check, release build, execution, and dynamic Chinese catalog
  installation; and
- the same registry flow on Ubuntu 24.04, macOS 15, and Windows 2025.

The local release gates used Node.js `26.7.0` exclusively. Native, Wasm,
Wasm-GC, and JavaScript passed 122/122, 122/122, 100/100, and 108/108 tests.
Runtime, generator, CLI, and core coverage passed at 90.3%, 87.9%, 87.3%, and
88.7%. All 117 public library items had documentation. Benchmark ratios were
0.39, 0.31, and 0.43 of their budgets. The packaged clean-module smoke and all
18 Chromium/Firefox/WebKit application scenarios passed.

This release completes the `0.2.x` authoring-and-diagnostics roadmap gate. It
retains catalog v2 and profile
`lampclaw-mf2-strict-v1+lampclaw-datetime-v1`; it does not claim complete
Unicode MessageFormat 2 conformance.

## Superseded registry artifact: `0.2.0`

`0.2.0` was published once from commit
`1fb8f442d6a0fc0e7278b814dfb7d617b973234c`. Its archive checksum was
`b7aa5b3d2c1a53a52fc26f6250c951412d6da0c3fc80153dd3a308af9404aa1e`.
The publish ran from `2026-08-14T03:23:23Z` through
`2026-08-14T03:23:25Z`, returned `200 OK`, and was not retried or overwritten.

Mooncakes build and documentation verification succeeded, but the first
exact-version smoke found four user-cache coordination locks: two unreachable
locks for scaffold staging paths and two correct locks for final output paths.
The application itself built and ran, but this violated the published lock
ownership contract. The signed local `v0.2.0` tag was therefore not pushed,
and `0.2.1` fixed the defect through a new immutable patch version.
