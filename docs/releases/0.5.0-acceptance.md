# `lampclaw/i18n` 0.5.0 acceptance record

Date: 2026-08-14

## Accepted release: `0.5.0`

Immutable identity:

- release commit: `e67fe243e47710743ae906d85fa359a6267ef3f5`;
- signed tag: `v0.5.0` (tag object
  `12a9186b607f6fee5e03633828beb23514c6c631`);
- tag signature: LampClaw ED25519 key
  `SHA256:YrC/9aayrFCWl773eSEhE6G3FgROFEc/VQmxTawowDA`;
- archive SHA-256:
  `c98730b5c1f82a3a9084669a470e0811a7354215f3a03fb3b8aeb0f945cfd67f`;
- release-commit Test workflow:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31774031941>;
- signed-tag Test workflow:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31774433545>;
- signed-tag Linux/macOS/Windows Registry smoke:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31774433523>; and
- registry page:
  <https://mooncakes.io/docs/#/lampclaw/i18n/0.5.0>.

The real `moon publish` command was invoked exactly once for `0.5.0`. It ran
from `2026-08-14T05:49:31Z` through `2026-08-14T05:49:33Z`, exited zero, and
reported `Server status: 200 OK`. No retry was attempted. The preceding dry
run passed both archive checks and received the exact version-specific `202
Accepted` success detail. The documented dry-run exit-255 compatibility path
was accepted only after all success evidence matched.

The local release runner used Moon `0.1.20260807`, moonc `0.10.7`, and
mooncake-bin `0.1.20260805`, providing an additional current-toolchain check.
The authoritative release-commit and signed-tag workflows independently used
the repository-pinned Moon `0.1.20260803` and moonc `0.10.6` toolchain and
passed. Node.js `26.7.0` was the only supported Node gate.

Mooncakes created the immutable version at
`2026-08-14T05:49:32.740596+00:00` and later reported build status `success`,
version `0.5.0`, the expected repository and Apache-2.0 license, and the exact
archive checksum above. Its rendered README, metadata, source archive,
generator API, runtime API, JavaScript formatter API, and prebuilt CLI asset
passed verification. The asynchronous build was allowed to finish without
retrying publication.

The exact-version clean-module smoke passed:

- `moon add lampclaw/i18n@0.5.0`;
- `moonx lampclaw/i18n/cmd/i18n@0.5.0` and optional `moon add --bin`;
- atomic bilingual scaffold with English embedded and Chinese dynamic;
- deterministic generation and deployment manifests with repeated true
  no-op behavior;
- JavaScript check, release build, execution, and independent dynamic Chinese
  namespace installation;
- exact SHA-256 verification of the generated dynamic catalog;
- the same registry flow on Ubuntu 24.04, macOS 15, and Windows 2025; and
- package inspection confirming that tests, fixtures, scripts, examples, and
  acceptance records are excluded from consumer archives.

Native, JavaScript, Wasm, and Wasm-GC passed 150/150, 136/136, 150/150, and
128/128 tests. Runtime, generator, CLI, and aggregate coverage passed at
91.5%, 86.7%, 86.4%, and 88.9%. All 155 public library items had documentation.
Benchmark ratios were approximately 0.39, 0.44, and 0.45 of their budgets.
The packaged clean-module smoke and all 18 Chromium/Firefox/WebKit application
scenarios passed. The Rabbita release JavaScript measured 205466 raw bytes and
48314 gzip bytes, with 2060 embedded-catalog bytes, a 1100-byte largest dynamic
chunk, and a 2123-byte deployment manifest, all inside published budgets.

This release completes the `0.5.x` stable Unicode MF2 syntax and interchange
data-model roadmap gate. It adds the separately versioned
`unicode-mf2-ldml48.2-syntax-v1` profile, complete pinned ABNF parsing,
normative public data-model and JSON interchange APIs, NFC-aware comparisons,
deterministic serialization, collision-safe variant signatures, and bounded
resource handling. The legacy catalog profile remains available without a
silent semantic change.

Conformance is anchored to Unicode MessageFormat 2 LDML 48.2 upstream commit
`d115a614079678850aac8b52742360e888b8f027`. All 114 accepted-syntax, 133
syntax-error, and 23 data-model fixtures vendored from that commit pass and are
protected by generated-file freshness and source checksum checks. This release
claims syntax and interchange data-model conformance for the named profile; it
does not yet claim complete MF2 declaration resolution, function selection,
fallback, bidirectional isolation, or formatting conformance. Those behaviors
are the `0.6.x` roadmap gate.
