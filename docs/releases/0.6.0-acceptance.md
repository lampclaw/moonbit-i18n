# `lampclaw/i18n` 0.6.0 acceptance record

Date: 2026-08-14

## Accepted release: `0.6.0`

Immutable identity:

- release commit: `bdb9beab6432591e028c9aed3f876b520b5546c5`;
- signed tag: `v0.6.0` (tag object
  `02f9b99b2de04a5f78e04f29c657f0869a8b531f`);
- tag signature: LampClaw ED25519 key
  `SHA256:YrC/9aayrFCWl773eSEhE6G3FgROFEc/VQmxTawowDA`;
- archive SHA-256:
  `b336438a2cadd072ac80589ad28f65f038c39629af32e3eca352a6191503e572`;
- release-commit Test workflow:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31777191833>;
- signed-tag Test workflow:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31777809133>;
- signed-tag Linux/macOS/Windows Registry smoke:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31777809132>; and
- registry page:
  <https://mooncakes.io/docs/#/lampclaw/i18n/0.6.0>.

The real `moon publish` command was invoked exactly once for `0.6.0`. It ran
from `2026-08-14T06:46:41Z` through `2026-08-14T06:46:43Z`, exited zero, and
reported `Server status: 200 OK`. No retry was attempted. The preceding dry
run passed both archive checks and received the exact version-specific `202
Accepted` success detail. The documented dry-run exit-255 compatibility path
was accepted only after all success evidence matched.

The local release runner used Moon `0.1.20260807`, moonc `0.10.7`, and
mooncake-bin `0.1.20260805`, providing an additional current-toolchain check.
The authoritative release-commit and signed-tag workflows independently used
the repository-pinned Moon `0.1.20260803` and moonc `0.10.6` toolchain and
passed. Node.js `26.7.0` was the only supported Node gate.

Mooncakes created the immutable version at
`2026-08-14T06:46:43.085669+00:00` and later reported build status `success`,
version `0.6.0`, the expected repository and Apache-2.0 license, and the exact
archive checksum above. Its rendered README, metadata, source archive,
generator API, runtime API, JavaScript formatter API, and prebuilt CLI asset
passed verification. The asynchronous build was allowed to finish without
retrying publication.

The exact-version clean-module smoke passed:

- `moon add lampclaw/i18n@0.6.0`;
- `moonx lampclaw/i18n/cmd/i18n@0.6.0` and optional `moon add --bin`;
- atomic bilingual scaffold with English embedded and Chinese dynamic;
- deterministic generation and deployment manifests with repeated true
  no-op behavior;
- JavaScript check, release build, execution, and independent dynamic Chinese
  namespace installation;
- public strict BCP 47 alias canonicalization and standalone MF2 formatting;
- exact SHA-256 verification of the generated dynamic catalog;
- the same registry flow on Ubuntu 24.04, macOS 15, and Windows 2025; and
- package inspection confirming that tests, fixtures, scripts, examples, and
  acceptance records are excluded from consumer archives.

Native, JavaScript, Wasm, and Wasm-GC passed 172/172, 158/158, 172/172, and
150/150 tests. Runtime, generator, CLI, and aggregate coverage passed at
91.1%, 86.7%, 86.4%, and 89.0%. All 179 public library items had documentation.
Benchmark ratios were approximately 0.39, 0.51, 0.36, and 0.43 of the compiled
format, standalone-model format, 10,000-message catalog lookup, and cached
Intl-number budgets. The packaged clean-module smoke and all 18
Chromium/Firefox/WebKit application scenarios passed. The Rabbita release
JavaScript measured 226095 raw bytes and 53892 gzip bytes, with 2060
embedded-catalog bytes, a 1100-byte largest dynamic chunk, and a 2123-byte
deployment manifest, all inside published budgets.

This release completes the `0.6.x` stable resolution, error, bidi, and locale
roadmap gate. It adds the separately versioned
`unicode-mf2-ldml48.2-resolution-v1` profile with source-ordered at-most-once
declaration resolution, normative multi-selector ranking, best-effort typed
errors and fallbacks, default Unicode bidi isolation, `u:id`/`u:dir`, and
renderer-independent structured markup. It also adds strict RFC 5646
canonicalization and RFC 4647 lookup boundaries generated from the pinned
IANA Language Subtag Registry dated `2026-08-08` with SHA-256
`be21e91b6851f750a7b1a687f11209d46ad5a8471d6b10a1efc8d1dac4c8a926`.
The legacy catalog profile and compatibility locale APIs remain available
without a silent semantic change.

Conformance is anchored to Unicode MessageFormat 2 LDML 48.2 upstream commit
`d115a614079678850aac8b52742360e888b8f027`. In addition to the 114 accepted
syntax, 133 syntax-error, and 23 data-model fixtures, all 67 vendored fallback,
pattern-selection, bidi, and Unicode-option cases pass with exact expected
text, error codes, and structured parts where specified. Generated-file
freshness and source checksums protect every fixture. This release claims the
named resolution-core profile, not complete MF2: stable default
`:string`/`:number`/`:integer`/`:date`/`:time`/`:datetime` functions, a public
custom function registry, and host CLDR formatting remain the `0.7.x` gate.
