# `lampclaw/i18n` 0.3.0 acceptance record

Date: 2026-08-14

## Accepted release: `0.3.0`

Immutable identity:

- release commit: `71f037f4fc64a95bd707358f54f67d7037cd9821`;
- signed tag: `v0.3.0`;
- tag signature: LampClaw ED25519 key
  `SHA256:YrC/9aayrFCWl773eSEhE6G3FgROFEc/VQmxTawowDA`;
- archive SHA-256:
  `e970f1313d3f4d7f5e82b69884ab6146054d3ce3539372230b514c381e0450d4`;
- release-commit Test workflow:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31769611352>;
- signed-tag Test workflow:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31770007185>;
- signed-tag Linux/macOS/Windows Registry smoke:
  <https://github.com/lampclaw/moonbit-i18n/actions/runs/31770007186>; and
- registry page:
  <https://mooncakes.io/docs/#/lampclaw/i18n/0.3.0>.

The real `moon publish` command was invoked exactly once for `0.3.0`. It ran
from `2026-08-14T04:25:13Z` through `2026-08-14T04:25:15Z`, exited zero, and
reported `Server status: 200 OK`. No retry was attempted. The preceding dry
run passed both archive checks and received the exact version-specific `202
Accepted` success detail. Moon `0.1.20260807` with mooncake-bin
`0.1.20260805` still mapped that successful dry run to exit 255; the release
helper accepted it only after all documented evidence matched.

Mooncakes later reported build status `success`, version `0.3.0`, the expected
repository and Apache-2.0 license, and the exact archive checksum above. Its
rendered README, metadata, source archive, generator API, runtime API,
JavaScript formatter API, and prebuilt CLI asset passed verification. The
asynchronous build was allowed to finish without retrying publication.

The exact-version clean-module smoke passed:

- `moon add lampclaw/i18n@0.3.0`;
- `moonx lampclaw/i18n/cmd/i18n@0.3.0` and optional `moon add --bin`;
- atomic bilingual scaffold with English embedded and Chinese dynamic;
- invalid MF2 repair cycle with JSON diagnostic `I18N3001`, source path, and
  span;
- deterministic generation manifest and repeated true no-op;
- exactly two final destination locks and no staging locks;
- JavaScript check, release build, execution, and dynamic Chinese catalog
  installation;
- state-aware XLIFF 2.1 export/import with exchange-state and loss-report
  sidecars;
- deterministic i18next JSON and Flutter ARB migration; and
- the same registry flow on Ubuntu 24.04, macOS 15, and Windows 2025.

The local release gates used Node.js `26.7.0` exclusively. Native, Wasm,
Wasm-GC, and JavaScript passed 129/129, 129/129, 107/107, and 115/115 tests.
Runtime, generator, CLI, and core coverage passed at 90.3%, 86.0%, 86.5%, and
87.5%. All 123 public library items had documentation. Benchmark ratios were
0.39, 0.32, and 0.43 of their budgets. The packaged clean-module smoke and all
18 Chromium/Firefox/WebKit application scenarios passed.

This release completes the `0.3.x` translation-lifecycle-and-interchange
roadmap gate. Canonical JSON remains the content source; versioned exchange
state is a sidecar. XLIFF 2.1 is the loss-aware round-trip path. i18next JSON
and Flutter ARB are deliberately one-way migration paths whose omitted
constructs are reported. PO/POT is not claimed because the roadmap evidence
gate has not been met.

This release retains catalog v2 and profile
`lampclaw-mf2-strict-v1+lampclaw-datetime-v1`; it does not claim complete
Unicode MessageFormat 2 conformance. Unsupported XLIFF inline XML and
extension elements are rejected rather than silently changed. Unsupported
attributes, comments, processing instructions, and extension namespaces are
reported as losses.
